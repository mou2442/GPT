# GPT

This repository now serves as a lightweight landing repo for older tool links and migration notes.

## Project Moved

The AV Constraint Translator has been split into its own repository:

- New repository: `https://github.com/mou2442/cue-weaver`

That repository now carries the dedicated app files, templates, cases, and follow-up iteration history.

## What Stays Here

- `README.md`: migration note
- `index.html`: lightweight landing page for the old repo

## Why

The original `GPT` repo started as a temporary multi-tool container. Once the AV translator gained its own templates, notes, and cases, it made more sense to let it evolve in a dedicated repository instead of continuing to share space with unrelated tools.
